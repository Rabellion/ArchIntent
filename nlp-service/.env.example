LARAVEL_URL=http://localhost:8000
INTERNAL_KEY=sk_internal_dev_f8a9b2c1d5e4f7g3h9k8m2n5
MODEL_NAME=all-MiniLM-L6-v2
TOP_K=10

# Voice transcription (see transcriber.py). Get a key at
# platform.openai.com/api-keys. Without this set, /transcribe returns a
# clear "not configured" error rather than crashing.
OPENAI_API_KEY=
# Hard cap on uploaded audio size, enforced before the file is sent to
# OpenAI (their own API limit is 25MB; kept lower here to bound cost).
MAX_AUDIO_BYTES=15728640
